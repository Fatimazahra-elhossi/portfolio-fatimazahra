<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fatima-Zahra Elhossi | AI Portfolio</title>
    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #fcfcfd;
            --white: #ffffff;
            --primary: #4f46e5;
            --primary-soft: #eef2ff;
            --text-main: #1e293b;
            --text-muted: #64748b;
            --border: #f1f5f9;
            --shadow: rgba(0, 0, 0, 0.03);
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: var(--bg);
            color: var(--text-main);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            height: 80px;
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--border);
            z-index: 1000;
            display: flex;
            align-items: center;
        }

        .nav-container {
            max-width: 1100px;
            margin: 0 auto;
            width: 100%;
            padding: 0 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 800;
            letter-spacing: -1px;
            color: var(--text-main);
        }

        .nav-links {
            display: flex;
            gap: 2.5rem;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--text-muted);
            font-size: 0.85rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            transition: var(--transition);
        }

        .nav-links a:hover {
            color: var(--primary);
        }

        /* Hero Section */
        section {
            padding: 100px 2rem;
            max-width: 1100px;
            margin: 0 auto;
        }

        .hero {
            display: grid;
            grid-template-columns: 1.2fr 0.8fr;
            gap: 4rem;
            align-items: center;
            padding-top: 160px;
        }

        .badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--primary-soft);
            color: var(--primary);
            padding: 0.5rem 1rem;
            border-radius: 100px;
            font-size: 0.7rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            margin-bottom: 2rem;
        }

        h1 {
            font-size: 4rem;
            line-height: 1.1;
            font-weight: 800;
            margin-bottom: 1.5rem;
            color: var(--text-main);
        }

        h1 span {
            color: var(--primary);
        }

        .hero-text {
            font-size: 1.125rem;
            color: var(--text-muted);
            margin-bottom: 2.5rem;
            max-width: 500px;
        }

        .cta-group {
            display: flex;
            gap: 1.5rem;
            align-items: center;
        }

        .btn-primary {
            background: var(--text-main);
            color: var(--white);
            padding: 1rem 2rem;
            border-radius: 16px;
            text-decoration: none;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: var(--transition);
            box-shadow: 0 10px 20px rgba(0,0,0,0.05);
        }

        .btn-primary:hover {
            background: var(--primary);
            transform: translateY(-2px);
        }

        .social-links {
            display: flex;
            gap: 1.5rem;
        }

        .social-links a {
            color: var(--text-muted);
            transition: var(--transition);
        }

        .social-links a:hover {
            color: var(--primary);
        }

        .hero-image-container {
            position: relative;
        }

        .profile-card {
            background: var(--white);
            border: 1px solid var(--border);
            aspect-ratio: 4/5;
            border-radius: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 20px 40px var(--shadow);
            overflow: hidden;
        }

        .floating-stats {
            position: absolute;
            bottom: -20px;
            left: -20px;
            background: var(--white);
            padding: 1.5rem 2rem;
            border-radius: 24px;
            border: 1px solid var(--border);
            box-shadow: 0 15px 30px var(--shadow);
            display: flex;
            gap: 2rem;
        }

        .stat-item h4 {
            font-size: 1.5rem;
            font-weight: 800;
        }

        .stat-item p {
            font-size: 0.65rem;
            font-weight: 800;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }

        /* Education & Skills */
        .grid-2 {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 5rem;
        }

        .section-title {
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 0.3em;
            color: var(--primary);
            margin-bottom: 2.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-weight: 800;
        }

        .timeline-item {
            position: relative;
            padding-left: 2rem;
            padding-bottom: 2.5rem;
            border-left: 2px solid var(--border);
        }

        .timeline-item::before {
            content: '';
            position: absolute;
            left: -7px;
            top: 0;
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: var(--white);
            border: 2px solid var(--primary);
        }

        .timeline-item h3 {
            font-size: 1.1rem;
            margin-bottom: 0.25rem;
        }

        .timeline-item .school {
            color: var(--primary);
            font-weight: 600;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }

        .timeline-item .date {
            font-size: 0.75rem;
            font-weight: 700;
            color: var(--text-muted);
            text-transform: uppercase;
        }

        /* Skills Grid */
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 1.5rem;
        }

        .skill-card {
            background: var(--white);
            padding: 2rem;
            border-radius: 32px;
            border: 1px solid var(--border);
            transition: var(--transition);
        }

        .skill-card:hover {
            box-shadow: 0 20px 40px var(--shadow);
            border-color: var(--primary-soft);
        }

        .skill-icon {
            width: 48px;
            height: 48px;
            background: var(--bg);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1.5rem;
            color: var(--text-main);
        }

        .skill-card:hover .skill-icon {
            background: var(--primary);
            color: var(--white);
        }

        .skill-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            margin-top: 1rem;
        }

        .tag {
            font-size: 0.7rem;
            font-weight: 700;
            padding: 0.3rem 0.8rem;
            background: var(--bg);
            color: var(--text-muted);
            border-radius: 100px;
        }

        /* Projects */
        .projects-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            margin-bottom: 3rem;
        }

        .filter-buttons {
            display: flex;
            gap: 0.5rem;
            background: #f1f5f9;
            padding: 0.4rem;
            border-radius: 14px;
        }

        .filter-btn {
            border: none;
            background: none;
            padding: 0.6rem 1.2rem;
            font-size: 0.75rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            color: var(--text-muted);
            cursor: pointer;
            border-radius: 10px;
            transition: var(--transition);
        }

        .filter-btn.active {
            background: var(--white);
            color: var(--primary);
            box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        }

        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 2rem;
        }

        .project-card {
            background: var(--white);
            border: 1px solid var(--border);
            border-radius: 32px;
            padding: 2rem;
            display: flex;
            flex-direction: column;
            transition: var(--transition);
        }

        .project-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 25px 50px var(--shadow);
        }

        .project-category {
            font-size: 0.65rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: var(--primary);
            margin-bottom: 1rem;
            display: block;
        }

        .project-card h3 {
            font-size: 1.25rem;
            margin-bottom: 1rem;
        }

        .project-desc {
            font-size: 0.9rem;
            color: var(--text-muted);
            margin-bottom: 1.5rem;
            flex-grow: 1;
        }

        /* Contact */
        .contact-box {
            background: var(--text-main);
            border-radius: 48px;
            padding: 5rem;
            text-align: center;
            color: var(--white);
            position: relative;
            overflow: hidden;
        }

        .contact-box h2 {
            font-size: 3rem;
            margin-bottom: 1.5rem;
        }

        .contact-methods {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-top: 3rem;
        }

        .contact-link {
            background: var(--white);
            color: var(--text-main);
            padding: 1.25rem 2.5rem;
            border-radius: 20px;
            text-decoration: none;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            transition: var(--transition);
        }

        .contact-link:hover {
            transform: scale(1.05);
            background: var(--primary-soft);
        }

        footer {
            text-align: center;
            padding: 4rem;
            font-size: 0.75rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.2em;
            color: var(--text-muted);
        }

        /* Responsive */
        @media (max-width: 900px) {
            .hero, .grid-2 {
                grid-template-columns: 1fr;
            }
            h1 { font-size: 3rem; }
            .hero-image-container { order: -1; }
            .nav-links { display: none; }
            .contact-box { padding: 3rem 1.5rem; }
            .contact-box h2 { font-size: 2rem; }
            .contact-methods { flex-direction: column; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="nav-container">
            <div class="logo">FE.</div>
            <div class="nav-links">
                <a href="#about">About</a>
                <a href="#skills">Skills</a>
                <a href="#projects">Projects</a>
                <a href="#contact">Contact</a>
            </div>
        </div>
    </nav>

    <!-- Hero -->
    <section class="hero" id="hero">
        <div>
            <div class="badge">
                <i data-lucide="sparkles"></i> Available for Internship
            </div>
            <h1>Fatima-Zahra <br><span>Elhossi</span></h1>
            <p class="hero-text">
                Master's student specialized in Information Systems & AI, passionate about Machine Learning, NLP, and Big Data ecosystems.
            </p>
            <div class="cta-group">
                <a href="#contact" class="btn-primary">
                    Contact Me <i data-lucide="chevron-right"></i>
                </a>
                <div class="social-links">
                    <a href="#"><i data-lucide="linkedin"></i></a>
                    <a href="#"><i data-lucide="github"></i></a>
                </div>
            </div>
        </div>
        <div class="hero-image-container">
            <div class="profile-card">
                <i data-lucide="brain" size="100" style="opacity: 0.1;"></i>
            </div>
            <div class="floating-stats">
                <div class="stat-item">
                    <h4>Master's</h4>
                    <p>Education</p>
                </div>
                <div class="stat-item">
                    <h4>4+</h4>
                    <p>Projects</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Education & Certs -->
    <section id="about">
        <div class="grid-2">
            <div>
                <h2 class="section-title"><i data-lucide="award"></i> Education</h2>
                <div id="education-list">
                    <!-- Dynamic Content -->
                </div>
            </div>
            <div>
                <h2 class="section-title"><i data-lucide="award"></i> Certifications</h2>
                <div id="cert-list">
                    <!-- Dynamic Content -->
                </div>
            </div>
        </div>
    </section>

    <!-- Skills -->
    <section id="skills">
        <div style="text-align: center; margin-bottom: 4rem;">
            <p class="section-title" style="justify-content: center;">Expertise</p>
            <h3 style="font-size: 2.5rem; font-weight: 800;">Technical Arsenal</h3>
        </div>
        <div class="skills-grid" id="skills-container">
            <!-- Dynamic Content -->
        </div>
    </section>

    <!-- Projects -->
    <section id="projects">
        <div class="projects-header">
            <div>
                <p class="section-title">Portfolio</p>
                <h3 style="font-size: 2.5rem; font-weight: 800;">Featured Work</h3>
            </div>
            <div class="filter-buttons">
                <button class="filter-btn active" onclick="filterProjects('all')">All</button>
                <button class="filter-btn" onclick="filterProjects('Professional')">Pro</button>
                <button class="filter-btn" onclick="filterProjects('Academic')">Academic</button>
            </div>
        </div>
        <div class="projects-grid" id="projects-container">
            <!-- Dynamic Content -->
        </div>
    </section>

    <!-- Contact -->
    <section id="contact">
        <div class="contact-box">
            <h2>Let's build the future <br> of AI together.</h2>
            <p style="opacity: 0.7; max-width: 500px; margin: 0 auto;">Currently seeking an internship to apply my expertise in Machine Learning and Data Science.</p>
            <div class="contact-methods">
                <a href="mailto:fatimazahraelhossi@gmail.com" class="contact-link">
                    <i data-lucide
